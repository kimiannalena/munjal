// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDHIIWek_wVTgnCxrHVaPYaq3gIcXo_DT0',
    authDomain: 'munjal-export-import.firebaseapp.com',
    projectId: 'munjal-export-import',
    storageBucket: 'munjal-export-import.appspot.com',
    messagingSenderId: '812278035242',
    appId: '1:812278035242:web:10bbde706d75b33c4a2453',
    measurementId: 'G-Q3GFMLCQEF'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
